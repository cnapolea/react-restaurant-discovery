{
  "name": "yummiapp",
  "version": "1.0.0",
  "description": "Yummi is a website that has as its final goal the discovery of existing and new restaurants, and it also serves as a social platform for users to share and explore experiences with these establishments.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently cd server && npm run dev && cd ../client && npm start"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
